╔═══════════════════════════════════════════════════════════════╗
║  🐳 OLLAMA NO DOCKER - Sem IP fixo!                          ║
╚═══════════════════════════════════════════════════════════════╝

Agora o Ollama roda como serviço Docker, sem precisar de IP fixo!

═══════════════════════════════════════════════════════════════
🚀 SETUP INICIAL (primeira vez)
═══════════════════════════════════════════════════════════════

# 1. Atualizar código
cd ~/poc_multiagent_lang
git pull origin main

# 2. Atualizar .env
cat > .env << 'EOF'
DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/regulatory_ai
LLM_TYPE=ollama
OLLAMA_BASE_URL=http://ollama:11434
OLLAMA_MODEL=llama2
EOF

# 3. Parar tudo (se estiver rodando)
docker compose down

# 4. Iniciar todos os serviços (incluindo Ollama)
docker compose up -d

# 5. Aguardar serviços iniciarem
echo "Aguardando 15 segundos..."
sleep 15

# 6. Baixar modelo llama2 no Ollama
echo "Baixando modelo llama2 (pode demorar alguns minutos)..."
docker compose exec ollama ollama pull llama2

# 7. Testar
curl -X POST http://localhost:8000/analyze \
  -H 'Content-Type: application/json' \
  -d '{"regulatory_text":"RESOLUÇÃO BCB Nº 789/2024","repo_path":"/app/fake_pix_repo"}'

───────────────────────────────────────────────────────────────

COPIE TODO O BLOCO ACIMA

═══════════════════════════════════════════════════════════════
✅ VANTAGENS DO OLLAMA NO DOCKER
═══════════════════════════════════════════════════════════════

✅ Sem IP fixo - usa nome do serviço (ollama:11434)
✅ Funciona após reiniciar o servidor
✅ Isolado em container
✅ Fácil de gerenciar
✅ Dados persistentes (volume ollama_data)
✅ Health check automático

═══════════════════════════════════════════════════════════════
🔄 USAR DEPOIS (já configurado)
═══════════════════════════════════════════════════════════════

# Iniciar tudo
docker compose up -d

# Parar tudo
docker compose down

# Ver logs do Ollama
docker compose logs ollama --tail 50

# Ver logs do backend
docker compose logs backend --tail 50

# Testar Ollama diretamente
docker compose exec ollama ollama list

═══════════════════════════════════════════════════════════════
📊 SERVIÇOS NO DOCKER COMPOSE
═══════════════════════════════════════════════════════════════

postgres:5432   - Banco de dados PostgreSQL
ollama:11434    - Ollama LLM (novo!)
backend:8000    - API Backend
frontend:8501   - Interface Streamlit

Todos se comunicam por nome de serviço, sem IP fixo!

═══════════════════════════════════════════════════════════════
🔍 VERIFICAR STATUS
═══════════════════════════════════════════════════════════════

# Ver todos os serviços
docker compose ps

# Deve mostrar:
# postgres  - Up (healthy)
# ollama    - Up (healthy)
# backend   - Up
# frontend  - Up

═══════════════════════════════════════════════════════════════
💾 DADOS PERSISTENTES
═══════════════════════════════════════════════════════════════

Os modelos do Ollama são salvos em um volume Docker:
- Volume: ollama_data
- Localização: /root/.ollama dentro do container

Mesmo se você fizer docker compose down, os modelos não são perdidos!

Para limpar tudo (cuidado!):
docker compose down -v  # Remove volumes também

═══════════════════════════════════════════════════════════════
🐛 TROUBLESHOOTING
═══════════════════════════════════════════════════════════════

Erro: "Ollama service not healthy"
→ Aguarde mais tempo: sleep 30
→ Veja logs: docker compose logs ollama

Erro: "Model not found"
→ Baixe o modelo: docker compose exec ollama ollama pull llama2

Erro: "Connection refused"
→ Verifique se Ollama está rodando: docker compose ps
→ Verifique .env: cat .env (deve ter http://ollama:11434)

═══════════════════════════════════════════════════════════════
📈 PERFORMANCE
═══════════════════════════════════════════════════════════════

Ollama no Docker tem a mesma performance que no host:
- Primeira requisição: 10-30s (carregando modelo)
- Requisições seguintes: 2-5s por token
- Tempo total: 60s+ para análise completa

Isso é normal para t3.small sem GPU.

═══════════════════════════════════════════════════════════════
🎯 RECOMENDAÇÃO
═══════════════════════════════════════════════════════════════

Use Ollama no Docker para:
✅ POC/Demo sem dependências externas
✅ Desenvolvimento local
✅ Ambientes sem acesso a APIs externas
✅ Quando não quer gastar com APIs

Use OpenAI/Gemini para:
✅ Produção com muitos usuários
✅ Quando precisa de respostas rápidas
✅ Quando tem orçamento para APIs

═══════════════════════════════════════════════════════════════
