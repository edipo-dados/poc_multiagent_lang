═══════════════════════════════════════════════════════════════
🔍 BACKEND CRASHOU - VERIFICAR LOGS
═══════════════════════════════════════════════════════════════

Connection reset = Backend crashou durante a requisição

═══════════════════════════════════════════════════════════════
📋 COMANDOS PARA DIAGNOSTICAR
═══════════════════════════════════════════════════════════════

# 1. Ver status dos containers
docker compose ps

# 2. Ver logs do backend (últimas 100 linhas)
docker compose logs --tail=100 backend

# 3. Ver se backend está rodando
docker compose ps backend

# 4. Se backend não está rodando, ver por que crashou
docker compose logs backend | tail -50

# 5. Reiniciar backend
docker compose restart backend

# 6. Ver logs em tempo real
docker compose logs -f backend

# 7. Testar novamente
curl -X POST http://localhost:8000/analyze \
  -H 'Content-Type: application/json' \
  -d '{"regulatory_text":"RESOLUÇÃO BCB Nº 789/2024","repo_path":"/app/fake_pix_repo"}'

═══════════════════════════════════════════════════════════════
🔧 POSSÍVEIS CAUSAS
═══════════════════════════════════════════════════════════════

1. Erro no código ao processar gemini-2.5-flash
2. Timeout na requisição ao Gemini
3. Memória insuficiente
4. Erro de sintaxe no .env
5. Bug no código do GeminiLLM

═══════════════════════════════════════════════════════════════
💡 SOLUÇÃO RÁPIDA
═══════════════════════════════════════════════════════════════

Execute os comandos acima e me envie:
1. Saída de: docker compose ps
2. Saída de: docker compose logs --tail=100 backend

Com isso posso identificar o problema exato!
