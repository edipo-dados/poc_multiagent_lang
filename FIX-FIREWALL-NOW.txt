╔═══════════════════════════════════════════════════════════════╗
║                                                               ║
║  🔥 FIX FIREWALL - EXECUTE AGORA NO EC2                      ║
║                                                               ║
╚═══════════════════════════════════════════════════════════════╝

PROBLEMA: Ollama está rodando mas o firewall bloqueia conexões na porta 11434

═══════════════════════════════════════════════════════════════
🚀 SOLUÇÃO RÁPIDA (copie e cole tudo de uma vez)
═══════════════════════════════════════════════════════════════

sudo iptables -I INPUT -s 172.16.0.0/12 -p tcp --dport 11434 -j ACCEPT
sudo iptables -I INPUT -s 172.31.0.0/16 -p tcp --dport 11434 -j ACCEPT
sudo iptables -I INPUT -s 127.0.0.1 -p tcp --dport 11434 -j ACCEPT

═══════════════════════════════════════════════════════════════
✅ VERIFICAR SE FUNCIONOU
═══════════════════════════════════════════════════════════════

# Testar do host
curl http://172.31.5.241:11434/api/tags

# Se funcionou, você verá uma lista de modelos JSON
# Se não funcionou, verá "Connection refused"

═══════════════════════════════════════════════════════════════
🔄 DEPOIS DO FIX, REINICIAR O BACKEND
═══════════════════════════════════════════════════════════════

docker compose restart backend
sleep 8
curl -X POST http://localhost:8000/analyze \
  -H 'Content-Type: application/json' \
  -d '{"regulatory_text":"Test","repo_path":"/app/fake_pix_repo"}'

═══════════════════════════════════════════════════════════════
📋 OU USE O SCRIPT AUTOMÁTICO
═══════════════════════════════════════════════════════════════

cd ~/poc_multiagent_lang
git pull origin main
bash fix-firewall.sh
docker compose restart backend

═══════════════════════════════════════════════════════════════
🔍 VERIFICAR REGRAS DO FIREWALL
═══════════════════════════════════════════════════════════════

# Ver todas as regras
sudo iptables -L INPUT -n --line-numbers

# Ver apenas porta 11434
sudo iptables -L INPUT -n | grep 11434

═══════════════════════════════════════════════════════════════
💾 TORNAR AS REGRAS PERMANENTES (opcional)
═══════════════════════════════════════════════════════════════

# Instalar iptables-persistent
sudo apt-get update
sudo apt-get install -y iptables-persistent

# Salvar regras atuais
sudo iptables-save | sudo tee /etc/iptables/rules.v4

# Agora as regras vão persistir após reboot

═══════════════════════════════════════════════════════════════
🎯 RESULTADO ESPERADO
═══════════════════════════════════════════════════════════════

ANTES (❌):
  curl http://172.31.5.241:11434/api/tags
  curl: (7) Failed to connect to 172.31.5.241 port 11434: Connection refused

DEPOIS (✅):
  curl http://172.31.5.241:11434/api/tags
  {"models":[{"name":"llama2:latest",...}]}

═══════════════════════════════════════════════════════════════
🐛 SE AINDA NÃO FUNCIONAR
═══════════════════════════════════════════════════════════════

1. Verificar se Ollama está escutando em 0.0.0.0:
   sudo netstat -tlnp | grep 11434
   
   Deve mostrar: 0.0.0.0:11434 (não 127.0.0.1:11434)
   
   Se mostrar 127.0.0.1, reinicie o Ollama:
   pkill ollama
   export OLLAMA_HOST=0.0.0.0:11434
   nohup ollama serve > /tmp/ollama.log 2>&1 &

2. Verificar logs do Ollama:
   cat /tmp/ollama.log

3. Verificar se há outros firewalls (UFW):
   sudo ufw status
   
   Se ativo:
   sudo ufw allow from 172.31.0.0/16 to any port 11434

═══════════════════════════════════════════════════════════════
📚 EXPLICAÇÃO
═══════════════════════════════════════════════════════════════

O que as regras fazem:
- 172.16.0.0/12: Permite redes Docker (172.16.x.x - 172.31.x.x)
- 172.31.0.0/16: Permite sua subnet EC2 específica
- 127.0.0.1: Permite localhost

Por que é necessário:
- Docker containers usam uma rede bridge separada
- Por padrão, o firewall bloqueia conexões entre redes
- Precisamos permitir explicitamente a porta 11434

═══════════════════════════════════════════════════════════════
