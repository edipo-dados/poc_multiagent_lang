╔═══════════════════════════════════════════════════════════════╗
║  🔨 REBUILD NECESSÁRIO - Código antigo no container          ║
╚═══════════════════════════════════════════════════════════════╝

O erro "Unknown LLM type: openai" significa que o container está usando
o código ANTIGO que não tem suporte para OpenAI.

Solução: Rebuild do container para pegar o novo código.

═══════════════════════════════════════════════════════════════
🚀 COMANDOS (copie tudo)
═══════════════════════════════════════════════════════════════

# 1. Parar tudo
docker compose down

# 2. Rebuild do backend
docker compose build backend

# 3. Iniciar tudo
docker compose up -d

# 4. Aguardar
echo "Aguardando 15 segundos..."
sleep 15

# 5. Verificar logs
docker compose logs backend --tail 30 | grep -E "(OpenAI|LLM|ERROR)"

# 6. Testar
curl -X POST http://localhost:8000/analyze \
  -H 'Content-Type: application/json' \
  -d '{"regulatory_text":"RESOLUÇÃO BCB Nº 789/2024","repo_path":"/app/fake_pix_repo"}' \
  | jq '.'

───────────────────────────────────────────────────────────────

COPIE TODO O BLOCO ACIMA

═══════════════════════════════════════════════════════════════
✅ RESULTADO ESPERADO
═══════════════════════════════════════════════════════════════

No passo 5 (logs):
✅ "Initialized OpenAILLM with model=gpt-3.5-turbo"
✅ "Sentinel Agent completed successfully"
✅ "Translator Agent completed successfully"

No passo 6 (teste):
✅ Resposta JSON em 2-5 segundos
✅ Sem timeout
✅ Dados estruturados completos

═══════════════════════════════════════════════════════════════
📋 O QUE ESTÁ ACONTECENDO
═══════════════════════════════════════════════════════════════

1. Você fez git pull ✅
2. O .env foi atualizado com LLM_TYPE=openai ✅
3. MAS o container ainda tem o código antigo ❌

Por quê?
- O Dockerfile copia o código para dentro da imagem
- Quando você faz git pull, o código no HOST muda
- Mas o código DENTRO do container não muda automaticamente
- Precisa rebuild para copiar o novo código

═══════════════════════════════════════════════════════════════
⏱️ TEMPO ESTIMADO
═══════════════════════════════════════════════════════════════

- docker compose build backend: 1-3 minutos
- docker compose up -d: 10-20 segundos
- Total: ~2-4 minutos

Vale a pena! Depois disso o Translator vai funcionar em 2-5s ao invés de 60s+

═══════════════════════════════════════════════════════════════
🐛 SE O BUILD FALHAR
═══════════════════════════════════════════════════════════════

Erro: "Cannot connect to Docker daemon"
→ Docker não está rodando
→ sudo systemctl start docker

Erro: "No space left on device"
→ Limpar imagens antigas
→ docker system prune -a

Erro: "Failed to fetch"
→ Problema de rede
→ Tente novamente

═══════════════════════════════════════════════════════════════
📊 VERIFICAÇÃO FINAL
═══════════════════════════════════════════════════════════════

Após o rebuild, execute:

# Ver versão do código no container
docker compose exec backend cat /app/backend/services/llm.py | grep -A 5 "class OpenAILLM"

# Deve mostrar a classe OpenAILLM
# Se não mostrar, o rebuild não funcionou

═══════════════════════════════════════════════════════════════
