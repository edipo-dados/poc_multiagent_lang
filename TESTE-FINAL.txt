╔═══════════════════════════════════════════════════════════════╗
║  🎯 TESTE FINAL - Pule o teste do container                  ║
╚═══════════════════════════════════════════════════════════════╝

O container não tem curl, mas não tem problema. Vamos direto ao teste da API.

═══════════════════════════════════════════════════════════════
🚀 EXECUTE ESTES COMANDOS:
═══════════════════════════════════════════════════════════════

# 1. Verificar .env
cat .env

# 2. Se não tiver o IP correto, atualizar:
cat > .env << 'EOF'
DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/regulatory_ai
LLM_TYPE=ollama
OLLAMA_BASE_URL=http://172.31.5.241:11434
OLLAMA_MODEL=llama2
EOF

echo "✅ .env atualizado"

# 3. Reiniciar backend
docker compose restart backend

# 4. Aguardar
echo "⏳ Aguardando 10 segundos para backend iniciar..."
sleep 10

# 5. Testar API
echo "═══════════════════════════════════════════════════════════"
echo "🧪 TESTE DA API:"
echo "═══════════════════════════════════════════════════════════"
curl -X POST http://localhost:8000/analyze \
  -H 'Content-Type: application/json' \
  -d '{"regulatory_text":"RESOLUÇÃO BCB Nº 789/2024 - Teste de regulação bancária sobre limites de crédito e requisitos de capital","repo_path":"/app/fake_pix_repo"}'

echo ""
echo "═══════════════════════════════════════════════════════════"

───────────────────────────────────────────────────────────────

COPIE TODO O BLOCO ACIMA E COLE NO TERMINAL

═══════════════════════════════════════════════════════════════
✅ RESULTADO ESPERADO
═══════════════════════════════════════════════════════════════

Você deve ver um JSON com:
{
  "execution_id": "...",
  "change_detected": true,
  "risk_level": "high",
  "regulatory_model": {...},
  "impacted_files": [...],
  "impact_analysis": {...},
  "technical_spec": "...",
  "kiro_prompt": "..."
}

SEM "Connection refused"!

═══════════════════════════════════════════════════════════════
❌ SE AINDA DER "Connection refused"
═══════════════════════════════════════════════════════════════

Ver logs do backend:
docker compose logs backend --tail 100 | grep -E "(Ollama|ERROR|Connection)"

Verificar se o Ollama ainda está rodando:
sudo ss -tlnp | grep 11434

Verificar variáveis de ambiente do container:
docker compose exec backend env | grep OLLAMA

═══════════════════════════════════════════════════════════════
📊 VERIFICAR LOGS DETALHADOS
═══════════════════════════════════════════════════════════════

docker compose logs backend --tail 50

Procure por:
✅ "Initialized OllamaLLM with model=llama2, base_url=http://172.31.5.241:11434"
✅ "Sentinel Agent completed successfully"
✅ "Translator Agent completed successfully"

❌ "Connection refused"
❌ "Ollama API call failed"

═══════════════════════════════════════════════════════════════
