🎯 COMANDO DEFINITIVO - ENTRE NO CONTAINER
===========================================

# Entre no container
docker compose exec backend bash

# Dentro do container, veja onde você está
pwd

# Liste o que tem
ls -la

# Procure o script
find . -name "populate_embeddings_asyncpg.py"

# Quando achar, rode com caminho completo
# Exemplo: se achar em ./backend/scripts/populate_embeddings_asyncpg.py
python ./backend/scripts/populate_embeddings_asyncpg.py

# OU se achar em ./scripts/populate_embeddings_asyncpg.py
python ./scripts/populate_embeddings_asyncpg.py

# Saia do container
exit

==========================================

SE NÃO ACHAR O ARQUIVO:
Você não fez rebuild! Rode:

docker compose down backend
docker compose up -d --build backend

E tente de novo.
