╔═══════════════════════════════════════════════════════════════╗
║  🔍 DIAGNÓSTICO COMPLETO - Execute para ver o problema        ║
╚═══════════════════════════════════════════════════════════════╝

Execute este bloco para diagnosticar o problema:

───────────────────────────────────────────────────────────────

echo "═══════════════════════════════════════════════════════════"
echo "1. Verificando em qual interface o Ollama está escutando:"
echo "═══════════════════════════════════════════════════════════"
sudo ss -tlnp | grep 11434
echo ""
echo "ESPERADO: 0.0.0.0:11434"
echo "PROBLEMA: 127.0.0.1:11434"
echo ""

echo "═══════════════════════════════════════════════════════════"
echo "2. Testando conexão do HOST para localhost:"
echo "═══════════════════════════════════════════════════════════"
curl -s http://localhost:11434/api/tags > /dev/null && echo "✅ localhost OK" || echo "❌ localhost FALHOU"
echo ""

echo "═══════════════════════════════════════════════════════════"
echo "3. Testando conexão do HOST para IP da rede:"
echo "═══════════════════════════════════════════════════════════"
curl -s http://172.31.5.241:11434/api/tags > /dev/null && echo "✅ IP OK" || echo "❌ IP FALHOU"
echo ""

echo "═══════════════════════════════════════════════════════════"
echo "4. Testando conexão do CONTAINER para IP da rede:"
echo "═══════════════════════════════════════════════════════════"
docker compose exec backend curl -s http://172.31.5.241:11434/api/tags > /dev/null && echo "✅ Container OK" || echo "❌ Container FALHOU"
echo ""

echo "═══════════════════════════════════════════════════════════"
echo "5. Verificando regras de firewall para porta 11434:"
echo "═══════════════════════════════════════════════════════════"
sudo iptables -L INPUT -n | grep 11434 || echo "Nenhuma regra encontrada"
echo ""

echo "═══════════════════════════════════════════════════════════"
echo "6. Verificando configuração do systemd:"
echo "═══════════════════════════════════════════════════════════"
if [ -f /etc/systemd/system/ollama.service.d/override.conf ]; then
    echo "Arquivo existe:"
    cat /etc/systemd/system/ollama.service.d/override.conf
else
    echo "❌ Arquivo NÃO existe"
fi
echo ""

echo "═══════════════════════════════════════════════════════════"
echo "7. Verificando variáveis de ambiente do Ollama:"
echo "═══════════════════════════════════════════════════════════"
sudo systemctl show ollama | grep Environment
echo ""

echo "═══════════════════════════════════════════════════════════"
echo "RESUMO DO DIAGNÓSTICO:"
echo "═══════════════════════════════════════════════════════════"
echo ""
echo "Se o item 1 mostrar 127.0.0.1:11434:"
echo "  → Ollama está escutando apenas em localhost"
echo "  → SOLUÇÃO: Configurar systemd (FIX-SYSTEMD.txt)"
echo ""
echo "Se o item 1 mostrar 0.0.0.0:11434 mas item 3 falhar:"
echo "  → Firewall está bloqueando"
echo "  → SOLUÇÃO: Adicionar regras de firewall"
echo ""
echo "Se o item 3 OK mas item 4 falhar:"
echo "  → Container não consegue alcançar o host"
echo "  → SOLUÇÃO: Problema de rede Docker"
echo ""

───────────────────────────────────────────────────────────────

COPIE TODO O BLOCO ACIMA E COLE NO TERMINAL

Depois me envie a saída completa para eu ver qual é o problema exato.

═══════════════════════════════════════════════════════════════
