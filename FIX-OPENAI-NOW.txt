╔═══════════════════════════════════════════════════════════════╗
║  🔧 FIX: Backend ainda usando Ollama                          ║
╚═══════════════════════════════════════════════════════════════╝

O backend precisa ser reconstruído com o novo código que suporta OpenAI.

═══════════════════════════════════════════════════════════════
🚀 COMANDOS PARA EXECUTAR NO EC2
═══════════════════════════════════════════════════════════════

# 1. Parar tudo
docker compose down

# 2. Verificar se .env está correto
cat .env

# Se não estiver com OpenAI, atualizar:
cat > .env << 'EOF'
DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/regulatory_ai
LLM_TYPE=openai
OPENAI_API_KEY=SUA_CHAVE_AQUI
OPENAI_MODEL=gpt-3.5-turbo
EOF

# IMPORTANTE: Substitua SUA_CHAVE_AQUI pela sua chave real!

# 3. Rebuild do backend (para pegar o novo código)
docker compose build backend

# 4. Iniciar tudo
docker compose up -d

# 5. Aguardar
echo "Aguardando 15 segundos..."
sleep 15

# 6. Verificar logs
docker compose logs backend --tail 30 | grep -E "(OpenAI|Ollama|LLM)"

# 7. Testar
curl -X POST http://localhost:8000/analyze \
  -H 'Content-Type: application/json' \
  -d '{"regulatory_text":"RESOLUÇÃO BCB Nº 789/2024","repo_path":"/app/fake_pix_repo"}' \
  | jq '.'

───────────────────────────────────────────────────────────────

COPIE TODO O BLOCO ACIMA (lembre de substituir SUA_CHAVE_AQUI)

═══════════════════════════════════════════════════════════════
✅ RESULTADO ESPERADO NOS LOGS
═══════════════════════════════════════════════════════════════

Deve mostrar:
✅ "Initialized OpenAILLM with model=gpt-3.5-turbo"

NÃO deve mostrar:
❌ "Initialized OllamaLLM with model=llama2"

═══════════════════════════════════════════════════════════════
🔍 VERIFICAR VARIÁVEIS DE AMBIENTE DO CONTAINER
═══════════════════════════════════════════════════════════════

docker compose exec backend env | grep -E "(LLM|OPENAI)"

Deve mostrar:
LLM_TYPE=openai
OPENAI_API_KEY=sk-proj-...
OPENAI_MODEL=gpt-3.5-turbo

Se não mostrar, o .env não está sendo lido corretamente.

═══════════════════════════════════════════════════════════════
🐛 SE AINDA MOSTRAR OLLAMA
═══════════════════════════════════════════════════════════════

# Verificar se o .env está no lugar certo
ls -la .env

# Ver conteúdo do .env
cat .env

# Forçar rebuild completo
docker compose down
docker compose build --no-cache backend
docker compose up -d

# Aguardar e verificar
sleep 15
docker compose logs backend --tail 50 | grep -i llm

═══════════════════════════════════════════════════════════════
📋 CHECKLIST
═══════════════════════════════════════════════════════════════

□ git pull origin main executado
□ .env atualizado com LLM_TYPE=openai
□ .env tem OPENAI_API_KEY com chave válida
□ docker compose down executado
□ docker compose build backend executado
□ docker compose up -d executado
□ Logs mostram "OpenAILLM" (não "OllamaLLM")
□ Teste da API retorna em 2-5 segundos (não 60s)

═══════════════════════════════════════════════════════════════
